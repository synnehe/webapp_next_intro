import CartProduct from "@/components/CartProduct"
import { render } from "@testing-library/react"


describe ("cart functionality", () => {
  it ("should increment quantity by click on '+' button"), () => {
    //render(<CartProduct />)
    //Kjøre funksjon
    //Expect to be...

  }
  it ("should decrement quantity by click on '-' button"), () => {

  }
  it ("should remove product by click on 'delete' button"), () => {

  }
  it ("should remove product if quantity is 0"), () => {

  }

}) 